import { ADD_TO_CART, SET_CART } from '../actions/types';
import addtocartReducer from './addtocartReducer';

describe('addtocartReducer', () => {
const initialState = [];
it('should update the state and ADD_TO_CART when type is SET_MENU and payload is specified', () => {
    // const action = {
    //   type: 'ADD_TO_CART',
    //   payload: [{ food_id: 5, quantity: 5 }],
    // };
    // expect(menuReducers(initialState, action)).toMatchSnapshot({});
    // expect(menuReducers(initialState, action)).toEqual({ 0: { food_id: 5, quantity: 5 } });
  });
});
